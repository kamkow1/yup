import "$std/io.bc";

fac(n: i32) i32 {
	if n == 0 {
		return 1;
	}

	return n * fac(n - 1);
}

main() i32 {

	var n: i32;
	scan_input("give me a number: %i", &n);
	fmt_print("%i! = %i", n, fac(n));

	return 0;
}
