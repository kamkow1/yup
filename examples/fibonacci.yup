import "$std/io.bc";

fib(n: i32) i32 {
	if n == 0 or n == 1 {
		return n;
	}

	return fib(n - 1) + fib(n - 2);
}

main() i32 {

	var n: i32;
	scan_input("give me a number: %i", &n);
	fmt_print("  fib(%i) is %i", n, fib(n));

	return 0;
}
