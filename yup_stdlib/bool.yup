#[friends("bool_to_string", "new_bool")]
Bool struct {
	value: i1;
}

const TRUE  = 1;
const FALSE = 0;

/* new_bool(b: i32) -> *Bool {
	const bl: *Bool = malloc(size_of('Bool));
	/*if b == TRUE {
		&bl.value = cast(TRUE, 'i1);
	} else {
		&bl.value = cast(FALSE, 'i1);
	}*/
	
	return bl;
}*/

/*bool_to_string(b: *Bool) -> *String {
	var str: *String = Null;
	if &b.value == cast(TRUE, 'i1) {
		str = new_string("True");
	} else {
		str = new_string("False");
	}
	
	return str;
}*/

